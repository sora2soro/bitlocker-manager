# BitLocker Manager — server dependencies (Python 3.11+)
# Install with:  pip install -r requirements.txt

# --- web service ---
fastapi>=0.111
uvicorn>=0.30
pydantic>=2.6

# --- data + crypto ---
sqlalchemy>=2.0
cryptography>=42
argon2-cffi>=23

# --- auth (tokens + MFA) ---
pyjwt>=2.8
pyotp>=2.9

# --- built-in self-check (python -m pytest) ---
pytest>=8
httpx>=0.27

# --- exports (xlsx) ---
openpyxl>=3.1
